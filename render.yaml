services:
  - type: web
    name: namusoo-backend
    env: python
    # 루트가 아닌 backend/requirements.txt 를 읽도록 지정
    buildCommand: pip install -r backend/requirements.txt
    # backend/app.py 를 실행하도록 경로 지정
    startCommand: python backend/app.py
    plan: free
